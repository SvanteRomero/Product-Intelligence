# Start from the same official Airflow image version specified in your docker-compose.yaml
FROM apache/airflow:2.11.0

# Install the Docker provider package
RUN pip install --no-cache-dir apache-airflow-providers-docker
